using System;

namespace SQLGeneration
{
    /// <summary>
    /// Adds an inner join between two joinable items to the command.
    /// </summary>
    public interface IInnerJoin : IJoin
    {
    }
}
