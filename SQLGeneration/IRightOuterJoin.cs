using System;

namespace SQLGeneration
{
    /// <summary>
    /// Adds a right outer join between two joinable items to the command.
    /// </summary>
    public interface IRightOuterJoin : IJoin
    {
    }
}
